# blendcalc

Poultry feed ration optimizer. Balance least-cost feed blends against published
nutrient targets, swap to cheaper feedstuffs without dropping below spec, and
translate every blend into a real cost per egg.

## Getting started

```
flutter pub get
flutter run --dart-define-from-file=dart_defines.json
```
